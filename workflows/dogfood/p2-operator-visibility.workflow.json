{
  "schema_version": 1,
  "kind": "workflow-plan",
  "title": "P2 Operator Visibility: Tasks #10 + #11",
  "goal": "Implement dogfood backlog items #10 (run-level merged live output) and #11 (compact live monitor/status command), each in an isolated worktree with two sequential review-and-fix passes.",
  "cwd": ".",
  "runner": "ccc",
  "ccc_runner": "@mimo25p",
  "ccc_output_mode": "stream-json",
  "permission_mode": "yolo",
  "startup_delay": 0,
  "max_agents": 4,
  "failure_retries": 1,
  "tags": [
    "dogfood",
    "p2-operator-visibility",
    "multi-agent-phase",
    "worktree-lanes"
  ],
  "decisions": [
    {
      "title": "Per-task worktree isolation",
      "rationale": "Each task (#10 and #11) runs in its own git worktree branch so implementation and review edits never conflict across tasks."
    },
    {
      "title": "Two sequential review-and-fix passes per task",
      "rationale": "Each task gets two reviewers who independently review AND fix issues. Reviewer A uses @glm51 for semantic/logic depth. Reviewer B uses @cx-reviewer for standards/coverage breadth. Both loop until their review returns PASS."
    },
    {
      "title": "Parallel impls, sequential reviews",
      "rationale": "Both impls run in parallel (same phase, no deps). Reviews run after their impl completes (explicit depends_on). Phase boundary between impl and review ensures impls finish before reviews start."
    }
  ],
  "phases": [
    {
      "name": "impl-phase",
      "title": "Implementation",
      "goal": "Implement both features in parallel worktree lanes.",
      "jobs": [
        {
          "name": "impl-10",
          "role": "implementer",
          "prompt": "You are already running as a ccc worker. Do not run ccc or spawn/delegate to another agent. Work in the current cwd.\n\nImplement dogfood backlog item #10: Run-level merged live output.\n\nWhat to build:\n- Add run-level live output that merges all live agent streams with stable colored agent-name prefixes.\n- The merged view should interleave output from multiple running agents, each line prefixed with the agent name in a distinct color.\n- Integrate into the TUI run detail view (workflow_tui_render.py make_run_detail).\n- Add tests in tests/test_workflow.py.\n\nCode standards:\n- No file over 2000 LOC (hard), flag over 1000 (soft).\n- No duplicated logic.\n- Meaningful names, no magic numbers.\n- One module, one responsibility.\n\nAfter implementing, run: WORKFLOW_TEST_MODE=fast python3 -m unittest tests.test_workflow\n\nCommit with message: feat(workflow): add run-level merged live output (#10)\n\nReturn strict JSON with keys: status (pass|blocked), summary (string), files_changed (array), tests_passed (bool), commit (string or null).",
          "worktree": {
            "branch": "workflow/p2/impl-10",
            "base": "HEAD",
            "merge_target": "main"
          },
          "write_scope": [
            "scripts/workflow_tui_render.py",
            "scripts/workflow_tui_activity.py",
            "scripts/workflow_tui.py",
            "scripts/workflow_tui_live.py",
            "tests/test_workflow.py",
            "tests/snapshots/"
          ]
        },
        {
          "name": "impl-11",
          "role": "implementer",
          "prompt": "You are already running as a ccc worker. Do not run ccc or spawn/delegate to another agent. Work in the current cwd.\n\nImplement dogfood backlog item #11: Compact live monitor/status command.\n\nWhat to build:\n- Add a low-noise `workflow status` / `workflow watch` view for lead agents and humans.\n- One compact row per run/agent with: status, PID, elapsed time, last event, latest output excerpt, tool-call count, token delta.\n- Optional --json output.\n- Avoid direct run.json dumps and broad log tails during active runs.\n- Avoid printing full process command lines by default (worker prompts are huge); show only PID/process group, runner type, elapsed time, and safe command basename unless --verbose.\n- Support long quiet waits without repeatedly opening helper sessions.\n- Add to the wf CLI wrapper (scripts/wf).\n- Add tests in tests/test_workflow.py.\n\nCode standards:\n- No file over 2000 LOC (hard), flag over 1000 (soft).\n- No duplicated logic.\n- Meaningful names, no magic numbers.\n- One module, one responsibility.\n\nAfter implementing, run: WORKFLOW_TEST_MODE=fast python3 -m unittest tests.test_workflow\n\nCommit with message: feat(workflow): add compact live monitor/status command (#11)\n\nReturn strict JSON with keys: status (pass|blocked), summary (string), files_changed (array), tests_passed (bool), commit (string or null).",
          "worktree": {
            "branch": "workflow/p2/impl-11",
            "base": "HEAD",
            "merge_target": "main"
          },
          "write_scope": [
            "scripts/workflow_ops.py",
            "scripts/workflow_health.py",
            "scripts/workflow_state.py",
            "scripts/workflow_tui.py",
            "scripts/wf",
            "tests/test_workflow.py"
          ]
        }
      ]
    },
    {
      "name": "review-phase",
      "title": "Review",
      "goal": "Two-pass review for each task: semantic then standards.",
      "jobs": [
        {
          "name": "review-10a",
          "role": "semantic reviewer + fixer",
          "depends_on": "impl-10",
          "ccc_runner": "@glm51",
          "prompt": "You are already running as a ccc @glm51 review worker. Do not run ccc, do not spawn/delegate to another agent. Work in the current cwd.\n\nReview-and-fix pass for Task #10 (run-level merged live output). The implementation was just committed in this worktree.\n\nReview focus:\n1. Semantic correctness\n2. Edge cases (0/1/N agents, failures)\n3. State lifecycle\n4. Test coverage\n5. No behavioral regression\n\nReview-and-fix loop: fix and commit until PASS.\n\nRun: WORKFLOW_TEST_MODE=fast python3 -m unittest tests.test_workflow\n\nReturn strict JSON: status, findings, tests_passed, commits, notes.",
          "worktree": {
            "branch": "workflow/p2/impl-10",
            "base": "HEAD",
            "merge_target": "main"
          },
          "write_scope": [
            "scripts/workflow_tui_render.py",
            "scripts/workflow_tui_activity.py",
            "scripts/workflow_tui.py",
            "scripts/workflow_tui_live.py",
            "tests/test_workflow.py",
            "tests/snapshots/"
          ]
        },
        {
          "name": "review-10b",
          "role": "standards reviewer + fixer",
          "depends_on": "review-10a",
          "ccc_runner": "@cx-reviewer",
          "prompt": "You are already running as a ccc @cx-reviewer review worker. Do not run ccc, do not spawn/delegate to another agent. Work in the current cwd.\n\nReview-and-fix pass for Task #10. Previous semantic review already applied.\n\nReview focus:\n1. Code standards (LOC, duplication, naming)\n2. Test quality\n3. Re-export completeness\n4. Previous reviewer's fixes\n\nReview-and-fix loop: fix and commit until PASS.\n\nRun: WORKFLOW_TEST_MODE=fast python3 -m unittest tests.test_workflow\n\nReturn strict JSON: status, findings, tests_passed, commits, notes.",
          "worktree": {
            "branch": "workflow/p2/impl-10",
            "base": "HEAD",
            "merge_target": "main"
          },
          "write_scope": [
            "scripts/workflow_tui_render.py",
            "scripts/workflow_tui_activity.py",
            "scripts/workflow_tui.py",
            "scripts/workflow_tui_live.py",
            "tests/test_workflow.py",
            "tests/snapshots/"
          ]
        },
        {
          "name": "review-11a",
          "role": "semantic reviewer + fixer",
          "depends_on": "impl-11",
          "ccc_runner": "@glm51",
          "prompt": "You are already running as a ccc @glm51 review worker. Do not run ccc, do not spawn/delegate to another agent. Work in the current cwd.\n\nReview-and-fix pass for Task #11 (compact live monitor/status command). The implementation was just committed in this worktree.\n\nReview focus:\n1. Semantic correctness\n2. Edge cases (no runs, paused, no agents)\n3. Output discipline (--json validity)\n4. CLI integration\n5. Test coverage\n\nReview-and-fix loop: fix and commit until PASS.\n\nRun: WORKFLOW_TEST_MODE=fast python3 -m unittest tests.test_workflow\n\nReturn strict JSON: status, findings, tests_passed, commits, notes.",
          "worktree": {
            "branch": "workflow/p2/impl-11",
            "base": "HEAD",
            "merge_target": "main"
          },
          "write_scope": [
            "scripts/workflow_ops.py",
            "scripts/workflow_health.py",
            "scripts/workflow_state.py",
            "scripts/workflow_tui.py",
            "scripts/wf",
            "tests/test_workflow.py"
          ]
        },
        {
          "name": "review-11b",
          "role": "standards reviewer + fixer",
          "depends_on": "review-11a",
          "ccc_runner": "@cx-reviewer",
          "prompt": "You are already running as a ccc @cx-reviewer review worker. Do not run ccc, do not spawn/delegate to another agent. Work in the current cwd.\n\nReview-and-fix pass for Task #11. Previous semantic review already applied.\n\nReview focus:\n1. Code standards (LOC, duplication, naming)\n2. Test quality\n3. CLI ergonomics\n4. Previous reviewer's fixes\n\nReview-and-fix loop: fix and commit until PASS.\n\nRun: WORKFLOW_TEST_MODE=fast python3 -m unittest tests.test_workflow\n\nReturn strict JSON: status, findings, tests_passed, commits, notes.",
          "worktree": {
            "branch": "workflow/p2/impl-11",
            "base": "HEAD",
            "merge_target": "main"
          },
          "write_scope": [
            "scripts/workflow_ops.py",
            "scripts/workflow_health.py",
            "scripts/workflow_state.py",
            "scripts/workflow_tui.py",
            "scripts/wf",
            "tests/test_workflow.py"
          ]
        }
      ]
    }
  ]
}
